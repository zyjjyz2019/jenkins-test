node {
    stage('Build') {
        sh 'git branch -r'
        sh 'ls'
        checkout scm
        echo 'Building dev v2.1'
        sh 'git branch -r'
        sh 'cat Jenkinsfile'
        sh 'ls'
    }
    stage('Test') {
        echo 'Building dev....'
        sh 'git branch'
        sh 'cat Jenkinsfile'
    }
    stage('Deploy') {
        echo 'Building dev....'
        sh 'git branch'
        sh 'cat Jenkinsfile'
    }
}
