node {
    stage('Build') {
        echo 'Building dev...'
        sh 'git branch'
        sh 'cat Jenkinsfile'
    }
    stage('Test') {
        echo 'Building dev...'
        sh 'git branch'
        sh 'cat Jenkinsfile'
    }
    stage('Deploy') {
        echo 'Building dev....'
        sh 'git branch'
        sh 'cat Jenkinsfile'
    }
}
