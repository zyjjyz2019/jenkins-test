node {
    stage('Build') {
        checkout scm
        echo 'Building dev v1.4'
        sh 'git branch'
        sh 'cat Jenkinsfile'
    }
    stage('Test') {
        echo 'Building dev....'
        sh 'git branch'
        sh 'cat Jenkinsfile'
    }
    stage('Deploy') {
        echo 'Building dev....'
        sh 'git branch'
        sh 'cat Jenkinsfile'
    }
}
