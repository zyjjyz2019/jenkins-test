node {
    stage('Build') {
        sh 'git branch'
        echo 'Building dev'
    }
    stage('Test') {
        checkout scm
        sh 'git branch'
        echo 'Testing dev'
    }
    stage('Deploy') {
        sh 'git branch'
        echo 'Deploying dev'
    }
}
