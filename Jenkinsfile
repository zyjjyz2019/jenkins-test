node {
    stage('Build') {
        //checkout scm
        sh 'cat Jenkinsfile'
        echo 'Building main 2.0'
    }
    stage('Test') {
        sh 'cat Jenkinsfile'
        echo 'Testing main'
    }
    stage('Deploy') {
        echo 'Deploying main'
    }
}
